<!-- What does this change, and why? Link the issue with #n if there is one. -->

## Checks

- [ ] Tests cover the change (DSP: synthetic-signal unit tests; rendering: golden images from scripted `AudioFrame` sequences).
- [ ] New source files start with a two-line `// ABOUTME:` comment.
- [ ] Preset PRs touch exactly one file under `presets/` and say what music it was tuned on.
- [ ] No code copied from projectM or any other LGPL/GPL source.
